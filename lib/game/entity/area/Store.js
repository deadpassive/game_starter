/*global namespace*/

var Area = namespace("Game.Entity.Area");
var Game = namespace("Game");
var GClass = namespace("GClass")

Area.Store = (function() {
    var $this = function() {
        
    };
    
    GClass.extend(Game.Entity.BaseEntity, $this);
    
    return $this;
})();